/**
 * PostCSS configuration used by Vite. It simply loads the
 * Tailwind CSS and Autoprefixer plugins. All other CSS
 * processing is handled by Vite automatically.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};